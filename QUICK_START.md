# Быстрый старт - Сборка .exe

## ✅ Все требования реализованы

1. ✅ Поля для ввода значений цифрами
2. ✅ Размер круга до 200px, по умолчанию 60px
3. ✅ Предпросмотр круга в настройках
4. ✅ Чекбокс "Запускать вместе с Windows"
5. ✅ Чекбокс "Показывать в трее"
6. ✅ Благозвучные названия настроек
7. ✅ Окно настроек при первом запуске

## 🚀 Сборка .exe файла

### Простой способ (Windows):

```bash
# Двойной клик по файлу:
build.bat
```

### Или через командную строку:

```bash
npm run make-exe
```

## 📦 Где найти готовый файл

После сборки:
- **Расположение:** `dist/win-unpacked/Timer Overlay.exe`
- **Использование:** Запустите `Timer Overlay.exe` - это portable версия

## 🧪 Тестирование

1. Скопируйте папку `dist/win-unpacked/` на другую машину (или просто запустите)
2. Запустите `Timer Overlay.exe`
3. Проверьте:
   - ✅ Окно настроек открывается автоматически
   - ✅ Все поля работают
   - ✅ Превью обновляется
   - ✅ Чекбоксы работают
   - ✅ Круг появляется на экране

## 📝 Примечание

Если сборка выдает ошибки с символическими ссылками - это не критично, файл все равно создается в `dist/win-unpacked/`.

